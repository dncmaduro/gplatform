export type Box = {
    icon: string, 
    content: string,
    isBold: boolean,
}

export type ProductDescription = {
    color?: string,
    size?: number,
    icon: string,
    title: string,
    content: string,
}